rules:
  - /tmp/bocchi:
      - cat:
          user: paml
          log:
            - read
      - cp:
          user: paml
  - /tmp/hosts:
      - tac:
          user: root
          pid: 123
          ppid: 1234
          cwd: /tmp/hehe
          log:
            - read
            - write
          perm: rwx
      - cp:
          user: paml
